---
description: OpenSpec workflow — active changes in PROJECT_STATE; timeline guards and verification
alwaysApply: true
---

# OpenSpec workflow (MidiLooper)

Spec-driven development for the **timeline data model** track and related changes. **Authoritative process detail:** [`docs/00-authority/DELIVERY_RULES.md`](docs/00-authority/DELIVERY_RULES.md). **Active change list:** [`docs/runtime/PROJECT_STATE.md`](docs/runtime/PROJECT_STATE.md) + scope [`CURRENT_WORK.md`](docs/runtime/CURRENT_WORK.md). Roadmap: [`ROADMAP.md`](docs/runtime/ROADMAP.md) (not implementation authority).

## Setup

- OpenSpec CLI: Node.js **>= 20** (`nvm use 20`), then `openspec --version`
- Project config: `openspec/config.yaml`
- Source of truth: `openspec/specs/` (archived behavior after `/opsx:archive`)
- Active work: `openspec/changes/<name>/` (proposal, specs deltas, design, tasks)
- Navigation: `docs/plans/openspec_integration_overview.md`
- Restart Cursor after `openspec init` / `openspec update` so slash commands appear

## Cursor commands

| Command | Use |
|---------|-----|
| `/opsx:propose <idea>` | New change — creates proposal, specs, design, tasks |
| `/opsx:explore` | Spike / investigate before proposing |
| `/opsx:apply` | Implement `tasks.md` for the active change |
| `/opsx:sync` | Reconcile specs with implementation drift |
| `/opsx:archive` | Merge delta specs into `openspec/specs/`, archive change folder |

## Deliverable sequence (timeline — locked)

M1–M7 **shipped**. M8 edit + pool-budget **archived** to `openspec/specs/`.

Post-M7 order (do not reorder without project decision):

1. **Persistence / overlay** (current scope — see CURRENT_WORK)
2. **Hardening** — playback-window polish
3. **JamRecorder** — JamAction / D3 (**not** D13 MIDI capture)
4. **M10** — full Jam entity + Scenes + SD
5. **D13 arrangement capture** — only after R1–R5 in `docs/plans/phase-3-multi-loop.md`

**Hard stop:** Do **not** start D13-style `jam-recording` before JamRecorder path. Parked: `openspec/changes/archive/20260617-parked-jam-recording-d13/`.

## Active changes

**Do not maintain a static list here** — it goes stale. Check:

1. [`docs/runtime/PROJECT_STATE.md`](docs/runtime/PROJECT_STATE.md)
2. `ls openspec/changes/` (exclude `archive/`)
3. `openspec/changes/<name>/tasks.md` before implementing

## Brownfield docs

Cite in OpenSpec artifacts; do not duplicate wholesale (see DELIVERY_RULES):

- `~/.cursor/plans/timeline_data_model_refactor_9cbaeb60.plan.md` — M1–M10 order
- `docs/DELIVERABLE_TRACKING.md` — shipped vs next
- `docs/Guides/LOOP_MIDI_STORAGE_AND_VALIDATION.md` — capture/stop/undo
- `docs/plans/phase-3-multi-loop.md` — jam requirements (D13 deferred)

## Naming

See `.cursor/rules/Naming-Vocabulary-Teensy-Looper.mdc` and global **action + scope** rule.
Reuse **passes**, **Capture**, **editPass**, **NoteEditSession**, slot, overdub, jam, JamAction, HITL baseline.

## Verification

| Domain | Gates |
|--------|--------|
| Timeline / capture / undo | `pio test -e native` + HITL baseline per `HITL-Test-Flow.mdc` |
| Note edit / overlap | Native `test_edit_apply` + HITL edit per `HITL-Edit-Test-Flow.mdc` |
| Persistence / overlay | Native persistence tests; manual overlay when UI/SD coupled |
| Live record display bugs | `openspec/changes/archive/2026-06-24-edit-record-display-length-mode/BUG.md` RC1–RC4 when in scope |

Teensy upload only when user confirms after firmware build (`teensy41-capture-serial`).

## Archived reference

Normative specs: `openspec/specs/` (timeline-epochs, timeline-passes, note-edit-session-undo, long-record-memory-headroom, multi-loop-slots, …).

Change history: `openspec/changes/archive/`.
