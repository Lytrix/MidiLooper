---
description: OpenSpec workflow — m8-edit active; m8-rename archived; jam deferred
alwaysApply: true
---

# OpenSpec workflow (MidiLooper)

This repo uses [OpenSpec](https://github.com/Fission-AI/OpenSpec) for spec-driven
development on the **timeline data model** track (`refactor/timeline-data-model`).

## Setup

- OpenSpec CLI: Node.js **>= 20** (`nvm use 20`), then `openspec --version`
- Project config: `openspec/config.yaml`
- Source of truth: `openspec/specs/` (archived behavior after `/opsx:archive`)
- Active work: `openspec/changes/<name>/` (proposal, specs deltas, design, tasks)
- Navigation: `docs/plans/openspec_integration_overview.md`

Restart Cursor after `openspec init` / `openspec update` so slash commands appear.

## Cursor commands

| Command | Use |
|---------|-----|
| `/opsx:propose <idea>` | New change — creates proposal, specs, design, tasks |
| `/opsx:explore` | Spike / investigate before proposing |
| `/opsx:apply` | Implement `tasks.md` for the active change |
| `/opsx:sync` | Reconcile specs with implementation drift |
| `/opsx:archive` | Merge delta specs into `openspec/specs/`, archive change folder |

## Deliverable sequence (timeline plan — locked)

M1–M7 are **shipped**. Post-M7 order:

1. **M8 edit** — `m8-edit` (**active** — `/opsx:apply`)
2. **Hardening** — pool-budget, playback-window polish (propose when M8 done)
3. **JamRecorder** — `jam-recorder` change (JamAction / D3, **not** D13 MIDI capture)
4. **M10** — full Jam entity + Scenes + SD
5. **D13 arrangement capture** — only after R1–R5 locked in phase-3 docs

Do **not** start D13-style `jam-recording` before M8. Parked reference:
`openspec/changes/archive/20260617-parked-jam-recording-d13/`.

## Active changes

**`m8-edit`** — Edit op-lists, NoteEditSession, retire `editFlat_`.  
Run `/opsx:apply` on `openspec/changes/m8-edit/tasks.md`.

**Parked:** `archive/2026-06-20-parked-edit-record-display-length-mode/` — display + length-mode UX (D1–D3 not started).

**Archived (bug evidence):** `note-move-pitch-overlap-flaky`, `lengthen-overlap-neighbor-restore`, `change-length-commit-rematerialize` (Track C insert/reorder deferred).

**Archived:** `openspec/changes/archive/2026-06-18-m8-rename/` (Take/Capture vocabulary).  
**Archived:** `openspec/changes/archive/2026-06-19-overlap-hidden-note-select/` (**Closed** 2026-06-20).  
**Archived:** `openspec/changes/archive/2026-06-20-note-edit-hitl-focus-restore/` (B1/B2 focus restore).  
**Archived:** `openspec/changes/archive/2026-06-20-edit-focus-selection-drift/` (recheck done; superseded by hitl-focus-restore).  
**Archived:** `openspec/changes/archive/2026-06-20-timeline-pass-model/` (**passes[]**).  
**Archived:** `openspec/changes/archive/2026-06-20-m8-pass-vocabulary/` (**edit pass** vocabulary).  
**Archived:** `openspec/changes/archive/2026-06-20-note-edit-modification-session/` (overlap A1 + B1).  
**Archived:** `openspec/changes/archive/2026-06-20-note-edit-focus-reads/` (Phase 2a–2d **focus.last**).  
**Archived:** `openspec/changes/archive/2026-06-20-note-move-pitch-overlap-flaky/` (overlap root-cause evidence).  
**Archived:** `openspec/changes/archive/2026-06-20-lengthen-overlap-neighbor-restore/` (overlap HITL evidence).  
**Archived:** `openspec/changes/archive/2026-06-20-change-length-commit-rematerialize/` (Track A+B; spec `openspec/specs/change-length-commit-rematerialize/`).  
**Parked:** `openspec/changes/archive/2026-06-20-parked-edit-record-display-length-mode/` (display + length-mode UX).

## Brownfield docs

Cite in OpenSpec artifacts; do not duplicate wholesale:

- `~/.cursor/plans/timeline_data_model_refactor_9cbaeb60.plan.md` — M1–M7 + post-M7 order
- `docs/DELIVERABLE_TRACKING.md` — shipped vs next
- `docs/Guides/LOOP_MIDI_STORAGE_AND_VALIDATION.md` — capture/stop/undo constraints
- `docs/plans/phase-3-multi-loop.md` — jam requirements (D13 deferred)

## Naming

See `.cursor/rules/Naming-Vocabulary-Teensy-Looper.mdc` and global **action + scope** rule.
Reuse **passes**, **Capture**, **editPass**, **NoteEditSession**, slot, overdub, jam, JamAction, HITL baseline.

## Verification

- **m8-edit:** full native matrix + HITL per `.cursor/rules/HITL-Test-Flow.mdc`
- **note-edit-modification-session:** native `test_edit_apply` + HITL edit baseline per child BUG specs
- Teensy upload only when user confirms after firmware build
